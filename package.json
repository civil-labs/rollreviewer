{
  "name": "rollreviewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "nx run-many -t dev --exclude=rollreviewer",
    "build": "nx run-many -t build --exclude=rollreviewer",
    "lint": "nx run-many -t lint --exclude=rollreviewer"
  },
  "devDependencies": {
    "nx": "23.2.1",
    "typescript": "~6.0.2"
  },
  "nx": {},
  "dependencies": {
    "@playwright/test": "^1.63.0"
  }
}